To run our project:
1) open frontend and backend in sperate terminal windows
2) run "npm install node-modules" in both windows
3) run "npm start" on the backend window
4) run "npm start" on the frontend window
